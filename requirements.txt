# Kern-Abhängigkeiten der finalen Pipeline (05_pipeline_final_kaskade.ipynb)
pandas>=2.0
numpy>=1.24
spacy>=3.7
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66

# Jupyter (Notebooks ausführen)
jupyterlab>=4.0

# spaCy-Sprachmodell separat laden:
#   python -m spacy download en_core_web_sm

# --- Optional, nur für die früheren Entwürfe unter archive/ ---
# scikit-learn>=1.3   # Random Forest (verworfen)
# ollama>=0.1         # LLM-Methode via Ollama (verworfen)
